import React, { Component } from 'react';

export default class Logo extends Component {
    render () {
        return (
            <h1 className="logo text-center mt-5 mb-5">Bucketlist</h1>
        );
    }
}